# Bolt's Journal

## 2024-05-22 - GitHub Actions Expression Optimization
**Learning:** Simplify complex expressions in GitHub Actions by leveraging native object projection.
**Action:** Replace `fromJson(toJson(...))` and `join` patterns with direct array operators like `contains(array, item)` where supported.
